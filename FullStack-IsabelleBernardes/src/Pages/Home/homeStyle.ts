import { styled } from "styled-components";

export const HomepageTemplate = styled.div`
  overflow-x: hidden;
  max-width: 100%;

  .home-recentWorkSection {
    position: relative;
    z-index: 7;

    h2 {
      font-size: 8em;
      white-space: nowrap;
    }

    h3 {
      font-size: 3em;
    }
  }
`;
